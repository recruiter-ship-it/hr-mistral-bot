{
  "mcpServers": [
    {
      "name": "filesystem",
      "description": "Работа с файлами: чтение, запись, поиск",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "./workspace"],
      "transport": "stdio",
      "enabled": true
    }
  ]
}
