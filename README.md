# HR Mistral AI Bot

Улучшенный Telegram‑бот для HR‑задач на базе Mistral AI.

## Возможности

- 🤑 **Mistral AI**: Использование мощных моделей `mistral‑large‑latest` и `pixtral‑12b‑2409`.
- 👁‍🗨 **Vision**: Анализ изображений и скриншотов.
- 📄 **PDF**: Качественный разбор PDF‑документов (резюме, отчёты).
- 📅 **Google Calendar**: Интеграция для планирования встреч и просмотра расписания.
- 🔍 **Интернет‑поиск**: При необходимости бот ищет информацию в сети (Serper API) и использует её при ответе.
- 🤑 **Память**: История диалога сохраняется в базе данных, что позволяет лучше понимать контекст.
- ⚡ **Async**: Быстрая работа на базе асинхронной архитектуры.

## Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/recruiter-ship-it/hr-mistral-bot.git
   cd hr-mistral-bot
   ```

2. Создайте виртуальное окружение и установите зависимости:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

3. Настройте API‑ключи в коде или через переменные окружения.

4. Для работы Google Calendar:
   - Получите `credentials.json` в Google Cloud Console.
   - Положите его в корневую папку проекта или передайте как base64 в переменной окружения.

## Запуск

```bash
python bot.py
```

При запуске с помощью GitHub Actions используется файл `.github/workflows/bot.yml`, который
запускает бота раз в 5 минут в течение нескольких часов. Убедитесь, что все
секреты (`MISTRAL_API_KEY`, `TELEGRAM_BOT_TOKEN`, `GOOGLE_CREDENTIALS`) настроены
в настройках репозитория.
